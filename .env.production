# Production storefront build
# API calls: use the host that actually serves /api/v1 (currently tokriii.com;
# server.tokriii.com returns 502 until Nginx proxies it to Node).
VITE_API_BASE_URL=https://tokriii.com/api/v1
